[package]
name = "consumer"
version.workspace = true
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
factorio-rs = { path = "../../crates/factorio-rs" }
provider = { path = "../provider" }
