[package]
name = "tracing_test"
version.workspace = true
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
factorio-rs = { workspace = true, features = ["tracing", "serde"] }
